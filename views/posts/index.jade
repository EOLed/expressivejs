extends ../layout

block content
  .container
    .row
      .span12
        h1 All posts
        ul
          for post in posts
            li
              a(href=url(post), title=post.title, alt=post.title) #{post.title}

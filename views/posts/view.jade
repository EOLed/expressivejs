extends ../layout

block content
  .container.post
    .row
      .span7
        h1.post-title #{post.title}
        .post-content
          p.post-date #{fromNow(post.created)}
          !{post.content}
